# Агрегация, объединение и аналитическая витрина

Пара автономна: готовый длинный файл находится в `data/raw/movies_long_checkpoint.csv`.

Основные операции: `groupby`, `agg`, `pivot_table`, `stack`, `unstack`, `merge`, `get_dummies`, сборка витрины и графики.
